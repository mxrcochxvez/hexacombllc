# Arena synthesis

Base: candidate C (role-owned kit).
Graft from A: generated allowlist idea, `Page` width tokens (`shell` / `narrow`), `lint:spacing` name beside `check:space`.
Reject B: `SpacingProps` bag, numeric `SpaceStep` on pages, `DensityProvider`, `Box`, extra Table/Text/Input surface.

Pages import seven components. They never pass pixels. Density is `data-density` on `Page`. `globals.css` is deleted after last caller, not wrapped.
